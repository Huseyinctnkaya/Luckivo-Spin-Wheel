/**
 * Çark ayarlarının varsayılan değerleri.
 *
 * Editör ile storefront proxy'si aynı kaynaktan okumalı: ayrıştıklarında
 * merchant editörde açık gördüğü bir ayarı backend kapalı sayıyor ve arada
 * hiçbir hata üretilmediği için fark edilmiyor.
 */

// Config'de anahtar yoksa geçerli olan değer. Editörde toggle açık
// göründüğü için varsayılan da açık.
export const DEFAULT_SYNC_TO_SHOPIFY_CUSTOMERS = true;
